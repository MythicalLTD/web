---
id: user-event
---

import Admonition from '@theme/Admonition';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# User Event

Remember that those actions are executed AFTER the pervious action succeeded!

<Tabs>
<TabItem value='onAdd' label='onAdd' default>
    Name: `user.onAdd`

    Description: `When a new user registered!`

    ```php title='Example'
    $eventHandler->on('user.onAdd', function ($userToken) {
        //Do whatever you think is good to do idk ;)!
    });
    ```

    Arguments:
* @param string $userToken The user token 

</TabItem>
</Tabs>
