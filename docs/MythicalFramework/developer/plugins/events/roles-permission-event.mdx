---
id: roles-permission-event
---

# Roles Permission Event

import Admonition from '@theme/Admonition';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs>
<TabItem value='Create' label='Create' default>
    Name: `roles_permissions.Create`

    Description: `Create a role permission.`

    ```php title='Example'
    $eventHandler->on('roles_permissions.Create', function (int $roleId, string $permission) {
        //Do whatever you think is good to do idk ;)!
    });
    ```

    Arguments
     * @param int $roleId The role id
     * @param string $permission The permission

</TabItem>

<TabItem value='Delete' label='Delete'>
    :::tip
        This event runs before the role permission gets deleted!
    :::
    
    Name: `roles_permissions.Delete`

    Description: `Delete a role permission in the database!`

    ```php title='Example'
    $eventHandler->on('roles_permissions.Delete', function (int $id) {
        //Do whatever you think is good to do idk ;)!
    });
    ```

    Arguments
     * @param int $id The role permission id

</TabItem>

<TabItem value="Update" label="Update"> 
    :::tip
        This event runs before the role permission gets updated!
    :::

    Name: `roles_permissions.Update`

    Description: `Update a role permission in the database!`

    ```php title='Example'
    $eventHandler->on('roles_permissions.Update', function (int $id,string $permission) {
        //Do whatever you think is good to do idk ;)!
    });
    ```

    Arguments
     * @param int $id The role permission id
     * @param string $permission The permission

</TabItem>

</Tabs>
