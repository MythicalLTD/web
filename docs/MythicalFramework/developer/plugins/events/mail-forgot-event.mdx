---
id: mail-forgot-event
---

# Mail Forgot Event

import Admonition from '@theme/Admonition';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs>
<TabItem value='Add' label='Add' default>
    Name: `mail.forgot.Add`

    Description: `Add a verification code.`

    ```php title='Example'
    $eventHandler->on('mail.forgot.Add', function (int $record_number) {
        //Do whatever you think is good to do idk ;)!
    });
    ```

    Arguments

| Argument         | Type  | Description      |
| ---------------- | ----- | ---------------- |
| `$record_number` | `int` | The database id! |

</TabItem>

<TabItem value='Delete' label='Delete' default>
    :::tip
        This even is getting executed before the log gets deleted so you can still grab its old content!
    :::
    Name: `mail.forgot.Delete`

    Description: `Remove a verification code.`

    ```php title='Example'
    $eventHandler->on('mail.forgot.Delete', function (int $id) {
        //Do whatever you think is good to do idk ;)!
    });
    ```

    Arguments

| Argument | Type     | Description            |
| -------- | -------- | ---------------------- |
| `$code`  | `string` | The code of the record |

</TabItem>
<TabItem value='DeleteAll' label='DeleteAll' default>

    Name: `mail.forgot.DeleteAll`

    Description: `Wipe the hole table!`

    ```php title='Example'
    $eventHandler->on('mail.forgot.DeleteAll', function () {
        //Do whatever you think is good to do idk ;)!
    });
    ```

</TabItem>
</Tabs>
