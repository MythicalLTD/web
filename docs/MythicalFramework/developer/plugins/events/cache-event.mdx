---
id: cache-event
---

# Cache Event

import Admonition from '@theme/Admonition';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs>
<TabItem value='createFile' label='createFile' default>
    Name: `cacheFile.createFile`

    Description: `This function runs when the cache file is getting created!`

    ```php title='Example'
    $eventHandler->on('cacheFile.createFile', function () {
        //Do whatever you think is good to do idk ;)!
    });
    ```

</TabItem>
<TabItem value='set' label='set' default>
    Name: `cacheFile.set`

    Description: `Set a value in the cache with a specified expiration time in seconds.`

    ```php title='Example'
    $eventHandler->on('cacheFile.set', function ($key, $value, $expiryTimestamp) {
        //Do whatever you think is good to do idk ;)!
    });
    ```
    Arguments

| Argument           | Description                                   | Type   |
| ------------------ | --------------------------------------------- | ------ |
| `$key`             | The name of the key                           | string |
| `$value`           | The value associated with the key             | string |
| `$expiryTimestamp` | The timestamp indicating when the key expires | string |

</TabItem>
<TabItem value='update' label='update' default>
    Name: `cacheFile.update`

    Description: `Update an existing cache entry with a new value and expiration time.`

    ```php title='Example'
    $eventHandler->on('cacheFile.update', function ($key, $value, $expiryTimestamp) {
        //Do whatever you think is good to do idk ;)!
    });
    ```
    Arguments

| Argument           | Description                                   | Type   |
| ------------------ | --------------------------------------------- | ------ |
| `$key`             | The name of the key                           | string |
| `$value`           | The value associated with the key             | string |
| `$expiryTimestamp` | The timestamp indicating when the key expires | string |

</TabItem>

<TabItem value='delete' label='delete' default>
    Name: `cacheFile.delete`

    Description: `Delete a cache entry by key.`

    ```php title='Example'
    $eventHandler->on('cacheFile.delete', function ($key) {
        //Do whatever you think is good to do idk ;)!
    });
    ```
    Arguments

| Argument           | Description                                   | Type   |
| ------------------ | --------------------------------------------- | ------ |
| `$key`             | The name of the key                           | string |

</TabItem>
<TabItem value='purge' label='purge' default>
    Name: `cacheFile.purge`

    Description: `Purges the entire cache!`

    ```php title='Example'
    $eventHandler->on('cacheFile.purge', function () {
        //Do whatever you think is good to do idk ;)!
    });
    ```

</TabItem>
<TabItem value='process' label='process' default>
    Name: `cacheFile.process`

    Description: `Process the entire cache!`

    ```php title='Example'
    $eventHandler->on('cacheFile.process', function () {
        //Do whatever you think is good to do idk ;)!
    });
    ```

</TabItem>
</Tabs>
