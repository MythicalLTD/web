---
id: roles-event
---

# Role Event

import Admonition from '@theme/Admonition';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs>
<TabItem value='Create' label='Create' default>
    Name: `roles.Create`

    Description: `Create a new role in the database!`

    ```php title='Example'
    $eventHandler->on('roles.Create', function (string $name, int $weight) {
        //Do whatever you think is good to do idk ;)!
    });
    ```

    Arguments
     * @param string $name The name of the role
     * @param int $weight The default is set to 1

</TabItem>

<TabItem value='Delete' label='Delete'>
    :::tip
        This event runs before the role gets deleted!
    :::
    
    Name: `roles.Delete`

    Description: `Delete a role in the database!`

    ```php title='Example'
    $eventHandler->on('roles.Delete', function (int $id) {
        //Do whatever you think is good to do idk ;)!
    });
    ```

    Arguments
     * @param int $id The role id

</TabItem>

<TabItem value="Update" label="Update"> 
    :::tip
        This event runs before the role gets updated!
    :::

    me: `roles.Update`

    Description: `Update a role in the database!`

    ```php title='Example'
    $eventHandler->on('roles.Update', function (int $id,string $name, int $weight) {
        //Do whatever you think is good to do idk ;)!
    });
    ```

    Arguments
     * @param int $id The role id
     * @param string $name The name of the role
     * @param int $weight The default is set to 1

</TabItem>

</Tabs>
