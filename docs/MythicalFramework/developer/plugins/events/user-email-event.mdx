---
id: user-email-event
---

# User Email Event

import Admonition from '@theme/Admonition';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs>
<TabItem value='saveEmail' label='saveEmail' default>
    Name: `userEmail.saveEmail`

    Description: `Save an email`

    ```php title='Example'
    $eventHandler->on('userEmail.saveEmail', function ($subject, $body, $from, $uuid, $email_id) {
        //Do whatever you think is good to do idk ;)!

    });
    ```

    Arguments:

     * @param string $subject The subject of the email
     * @param string $body The body of the email
     * @param string $from The from of the email
     * @param string $uuid The uuid of the user

</TabItem>
<TabItem value='deleteEmail' label='deleteEmail'>
    :::tip
        This event gets triggered before the email gets deleted!
    :::
    
    Name: `userEmail.deleteEmail`

    Description: `Delete an email!`

    ```php title='Example'
    $eventHandler->on('userEmail.deleteEmail', function ($uuid, $id) {
        //Do whatever you think is good to do idk ;)!
    });
    ```

    Arguments:
     * @param string $uuid The user's UUID
     * @param string $id The email ID

</TabItem>
<TabItem value='deleteAllEmails' label='deleteAllEmails'>
    Name: `userEmail.deleteAllEmails`

    Description: `Delete all emails`

    ```php title='Example'
    $eventHandler->on('userEmail.deleteAllEmails', function ($uuid) {
        //Do whatever you think is good to do idk ;)!
    });
    ```

    Arguments:
     * @param string $uuid The user's UUID

</TabItem>
<TabItem value='markMailAsRead' label='markMailAsRead'>
    Name: `userEmail.markMailAsRead`

    Description: `Mark a mail as read!`

    ```php title='Example'
    $eventHandler->on('userEmail.markMailAsRead', function ($uuid, $id) {
        //Do whatever you think is good to do idk ;)!
    });
    ```

    Arguments:
     * @param string $uuid The user's UUID
     * @param string $id The email ID

</TabItem>
</Tabs>
